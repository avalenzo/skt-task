spring:
  rabbitmq:
    host: localhost
    port: 5673
    username: ${rabbit-user}
    password: ${rabbit-pass}
  datasource:
    url: jdbc:postgresql://localhost:5432/skttask
    username: postgres
    password: n1*3Fail
    driver-class-name: org.postgresql.Driver
  main:
    web-environment: false


